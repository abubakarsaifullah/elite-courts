# Original slider source images

These are the original uploaded source images. They are kept outside `public/` so the website does not deploy large unoptimized camera files.

The live homepage slider uses optimized WebP files in:

```txt
public/images/slider/
```

When replacing slider images, export optimized WebP files for the live site and keep any large camera originals here only if you need to archive them.
